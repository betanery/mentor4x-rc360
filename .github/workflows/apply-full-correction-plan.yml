name: Apply Full Correction Plan

on:
  push:
    branches:
      - hardening/full-correction-plan
    paths:
      - .github/workflows/apply-full-correction-plan.yml

permissions:
  contents: write

jobs:
  apply:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout hardening branch
        uses: actions/checkout@v4
        with:
          ref: hardening/full-correction-plan
          fetch-depth: 0

      - name: Apply validated correction patch
        shell: bash
        run: |
          set -euo pipefail
          base64 -d .hardening/full-correction-valid.patch.gz.b64 > /tmp/full-correction.patch.gz
          gunzip -c /tmp/full-correction.patch.gz > /tmp/full-correction.patch
          git apply --check /tmp/full-correction.patch
          git apply /tmp/full-correction.patch
          rm -rf .hardening/full-correction
          rm -f .hardening/full-correction-valid.patch.gz.b64

      - name: Commit applied corrections
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: apply full authorization and contract hardening plan"
          git push origin HEAD:hardening/full-correction-plan

# trigger: validated patch application

diff --git a/e2e/access-by-role.spec.ts b/e2e/access-by-role.spec.ts
index 3668e19..62c1330 100644
--- a/e2e/access-by-role.spec.ts
+++ b/e2e/access-by-role.spec.ts
@@ -68,12 +68,11 @@ for (const role of Object.keys(labels) as Role[]) {
     test("respeita as áreas reservadas por perfil", async ({ page }) => {
       const forbidden = await isolateNetwork(page);
       await loginAs(page, role);
-      const isStaff = ["super_admin", "mentor", "estrategista"].includes(role);
       const isConsultor = ["super_admin", "mentor"].includes(role);
 
-      for (const route of staffRoutes) {
+      for (const [route, allowedRoles] of Object.entries(routeRoles)) {
         await page.goto(route);
-        if (isStaff) await expect(page).toHaveURL(new RegExp(`${route.replace("/", "\\/")}$`));
+        if (allowedRoles.includes(role)) await expect(page).toHaveURL(new RegExp(`${route.replace("/", "\\/")}$`));
         else await expect(page).toHaveURL(/\/$/);
       }
       await page.goto("/mentor");
diff --git a/supabase/functions/socio-tools/index.ts b/supabase/functions/socio-tools/index.ts
index 27703dc..ea0f113 100644
--- a/supabase/functions/socio-tools/index.ts
+++ b/supabase/functions/socio-tools/index.ts
@@ -2,10 +2,6 @@ import { createClient } from "https://esm.sh/@supabase/supabase-js@2";
 import { getCompanyAuthorization } from "../_shared/company-authorization.ts";
 import { corsHeadersFor, rejectDisallowedOrigin } from "../_shared/cors.ts";
 
-
-const json = (body: unknown, status = 200) =>
-  new Response(JSON.stringify(body), { status, headers: { ...corsHeaders, "Content-Type": "application/json" } });
-
 /** Alçada mínima exigida por ferramenta (Consultor 4X detém as decisões metodológicas). */
 const REQUIRED_SCOPE: Record<string, "membro" | "estrategista" | "consultor"> = {
   create_goal: "consultor",
@@ -114,6 +110,8 @@ const tools = [
 Deno.serve(async (req) => {
   const corsHeaders = corsHeadersFor(req);
   if (!corsHeaders) return rejectDisallowedOrigin();
+  const json = (body: unknown, status = 200) =>
+    new Response(JSON.stringify(body), { status, headers: { ...corsHeaders, "Content-Type": "application/json" } });
   if (req.method === "OPTIONS") return new Response(null, { headers: corsHeaders });
   try {
     const authHeader = req.headers.get("Authorization");
